/target
/arche-core/target
# the cargo home shell.nix puts in the tree. Its contents rather than the
# directory, since git cannot name a file back in under an excluded one
.cargo/*
!.cargo/config.toml
.rustup

__pycache__/

# profiling
perf.data*
callgrind.out*
dhat.out*
flamegraph.svg*

# agent instructions for one machine, kept out of the repository
/AGENTS.local.md
/CLAUDE.local.md

# the harvested games and the corpus built from them: hundreds of megabytes,
# which the documented harvest writes into the working tree
/runs/
/corpus.epd
