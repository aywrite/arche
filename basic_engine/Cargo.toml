[package]
name = "basic_engine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
publish = false

# cfg(kani) is set by the model checker rather than by cargo, so cargo has to be
# told it is a name it should expect. See docs/VERIFICATION.md.
[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }

[lib]
bench = false #https://bheisler.github.io/criterion.rs/book/faq.html#cargo-bench-gives-unrecognized-option-errors-for-valid-command-line-options

[dependencies]
lazy_static = "1.5.0"
rand = { version = "0.9.2", features = ["small_rng"]}
smallvec = "1.15.1"

[dev-dependencies]
proptest = "1.7"
criterion = "0.7"
pretty_assertions = "1.4"

[[bench]]
name = "benchmark"
harness = false
