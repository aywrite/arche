# What the script tests need, so that ci installs the same thing a clone does.
# Compatible release pins rather than exact ones: what is held still is the
# major version the tests were written against. The linting is pinned by
# .pre-commit-config.yaml.
pytest~=9.1
pyyaml~=6.0
# the same release the lichess image builds the opening book with
chess~=1.11
numpy~=2.5
