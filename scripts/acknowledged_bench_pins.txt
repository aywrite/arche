# Commits on master whose stated bench does not match the counts their own
# tree pins. check_bench_pins.py reports these and steps over them, because a
# landed commit cannot be rewritten and one lapse would otherwise fail every
# release after it.
#
# One `<sha> <stated> <pinned>` line each. The numbers are part of the entry,
# so a commit whose figures are not these two is not acknowledged. An entry is
# added when the lapse is understood, not to get a release out.
#
# All four below were rebased before they landed, so the commit built by the
# bench workflow was not the commit that arrived.

# 2026-08-25, the pair the check was written from
f1f0730 36130893 35561814
5b12f03 36130893 35561814

# 2026-09-07, the pair the 0.4.2 changelog notes as lapses beside their entries
ac47935 4395471 4162584
a0bf845 4395471 4162584
