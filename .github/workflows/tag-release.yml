name: Tag release

# Tags master whenever the version in Cargo.toml has not been released yet,
# which is when a release pull request from prepare-release.yml has merged.
#
# The version rather than the commit message, because the merged commit's
# message depends on whether the pull request was merged, squashed or rebased.

on:
  push:
    branches: [ "master" ]

permissions:
  contents: write
  # needed to start the release workflow
  actions: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Work out whether there is anything to tag
        id: version
        run: |
          version=$(python3 -c "import tomllib; print(tomllib.load(open('Cargo.toml','rb'))['workspace']['package']['version'])")
          tag="v${version}"
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"
          if git rev-parse -q --verify "refs/tags/${tag}" >/dev/null; then
            echo "::notice::${tag} is already tagged, nothing to do"
            echo "needed=false" >> "$GITHUB_OUTPUT"
          else
            echo "needed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Create and push the tag
        if: steps.version.outputs.needed == 'true'
        env:
          TAG: ${{ steps.version.outputs.tag }}
        run: |
          git config user.name "${{ github.actor }}"
          git config user.email "${{ github.actor_id }}+${{ github.actor }}@users.noreply.github.com"
          git tag -a "$TAG" -m "chore: Release $TAG"
          git push origin "$TAG"

      # a tag pushed with GITHUB_TOKEN does not trigger the release workflow's
      # tag filter, so that workflows cannot set each other off.
      # workflow_dispatch is the documented exception.
      - name: Start the release workflow
        if: steps.version.outputs.needed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh workflow run release.yml --ref "${{ steps.version.outputs.tag }}"

      - name: Summary
        if: steps.version.outputs.needed == 'true'
        run: |
          {
            echo "### Tagged ${{ steps.version.outputs.tag }}"
            echo
            echo "The release workflow is building the binaries and playing the"
            echo "strength match, which takes a while. It will add the result"
            echo "to the release notes when it finishes."
          } >> "$GITHUB_STEP_SUMMARY"
